// const securePassword = prompt("Enter a password!");

// if (securePassword.length >= 8 && securePassword.indexOf(" ") === -1) {
//   alert("This looks secure");
// } else {
//   alert(
//     "Try with another password, it requires at least 8 characters, also no spaces are allowed."
//   );
// }

// const mystery = "Planet7";

// if (mystery[0] === "P" && mystery.length > 5 && mystery.indexOf("7") !== -1) {
//   console.log("YOU GOT IT!!!");
// }
