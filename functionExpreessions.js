function summerMachine(x, y, z, a, b) {
  return x + y + z + a + b;
}

let summerMachine2 = function (xx, yy, zz, aa, bb) {
  return xx + yy + zz + aa + bb;
};

let square = function (x) {
  return x * x;
};

square(5);
//25
